# To be moved to /etc/netplan/
# This file is generated from information provided by the datasource.  Changes
# to it will not persist across an instance reboot.  To disable cloud-init's
# network configuration capabilities, write a file
# /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg with the following:
# network: {config: disabled}
network:
    version: 2
    ethernets:
        eth0:
            optional: true
            dhcp4: true
    wifis:
        wlan0:
            optional: true
            access-points:
               "robot_group9":
                   password: "group9_LQBRH"
            dhcp4: true
        wlan0:
            optional: true
            access-points:
                "SharingsCaring":
                   password: "fpat3131"
            dhcp4: true
        wlan0:
            optional: true
            access-points:
               "Resistance0":
                   password: "J0hnC0nn0r"
            dhcp4: true
        wlan0:
            optional: true
            access-points:
               "Resistance1":
                   password: "J0hnC0nn0r"
            dhcp4: true
        wlan0:
            optional: true
            access-points:
               "Resistance2":
                   password: "J0hnC0nn0r"
            dhcp4: true